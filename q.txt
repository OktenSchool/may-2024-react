"При формуванні сторінки PostsWithComments я використав компоненти Post і Comment https://i.imgur.com/2WsHRc2.png.
 Сторінка рендериться, але при частих перемиканнях між вкладками інколи виводяться тільки пости без коментарів.
Я правильно розумію, що це відбувається через те, що при наповненні store ми кожний раз робимо асинхронний запит для отримання постів і коментарів, які можуть    відпрацьовувати не в тому порядку, як прописані?
І це треба враховувати при роботі з redux."


Чи є якісь мінуси в використанні module.css, чи вони не нагружають сильно сайт, використовувати module css і не паритись чи ліпше придумовувати назви класів самому щоб вони не повторювались.


Питання не по темі, чи нормальною є практика в react коли там декілька html тегів а не лише root,
дивився пару уроків по реакт на ютюб, там щоб створити модальне окно рекомендують додати ще один дів біля div root і туда додавати компонент з модальним окном за допомогою методу портал,
тому що модалки мають бути зверху в дом структурі


Добрий вечір) Консультацію буду дивитись у записі, проте хотіла б спитати таке:
коли я хочу у компоненті postsWithComments з хука useAppSelector повернути повністю стейт і деструктурувати пости і коменти, мені чомусь у консолі з'являється така помилка :
Selector unknown returned the root state when called. This can lead to unnecessary rerenders. Selectors that return the entire state are almost certainly a mistake, as they will cause a rerender whenever *anything* in state changes. {stack: 'Error \n at http://Loca Lhost: 3eee/static/js/bundL. tp://LocaLhost : 3000/stati c/js/bundle.js: 26164 :22), чи не могли б ви підказати чому так?


В мене вибиває помилку коли я оптимізую api.service згідно preview - react ts demos redux toolkit asyncThunk (9).  В extraReducers зразу починає підкреслювати addCase. На даний момент я цю частину api.service закоментувала і продублювала код як робила до того, тоді нічого не підкреслює. Можете глянути і підказати чому саме так?





В мене вибиває помилку коли я оптимізую api.service згідно preview - react ts demos redux toolkit asyncThunk (9).  В extraReducers зразу починає підкреслювати addCase. На даний момент я цю частину api.service закоментувала і продублювала код як робила до того, тоді нічого не підкреслює. Можете глянути і підказати чому саме так?


"1.Після створення слайсів та конфігуру стора.
Провожу фінальні дії стора.А саме після useDispatch. хочу ввести withTypes але не вдається.
Пише що в useDispatch не існує withTypes та пропоную створити функцію власний хук.

2.Чи є різниця як і де визначати <Provider store> та <RouterProvider router>
можливо це і є проблема першого питання але не певен.
"
